// Allowlisted in scrubEvent; keep in sync to avoid silent redaction.
export const SW_NOT_REGISTERED = "No service worker registered";
export const SW_REGISTRATION_TIMEOUT = "Service worker registration timeout";
